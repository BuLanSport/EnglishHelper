# 英译通 EnglishHelper 依赖清单（Windows / Python 3.10+）
# 一键安装：pip install -r requirements.txt

# 程序运行必需
PySide6>=6.6
keyboard>=0.13.5
requests>=2.28

# 可选：重新生成图标（gen_icon.py）用
Pillow>=10.0

# 可选：打包 exe（build.bat）用，取消下行注释即可安装
# pyinstaller>=6.0
